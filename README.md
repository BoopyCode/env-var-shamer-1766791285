# Environment Variable Shamer

Scans your code for hardcoded secrets that should be environment variables. Run: `python env_shamer.py .` to check current directory. Your secrets deserve better than being committed to Git!